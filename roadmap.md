# Smart Product Table – Plugin Roadmap

## Phase 1 – MVP (Free Plugin)
- ✅ Core plugin setup with namespace, class loader
- ✅ Basic shortcode support
- ✅ Table structure with column builder (SKU, price, title, image, etc.)
- ✅ Mobile responsive layout with basic styles
- ✅ Quantity and add-to-cart support
- ✅ Variation support (dropdown inside table)
- ✅ Basic filters (category, tag)
- ✅ AJAX pagination and search
- ✅ Product metabox with grouped tabbed fields (General, Design)
- Add product query controls (category, tag, product ID include/exclude)
- Table design options (bordered, striped, compact layout toggle)
- Toggleable table elements (search box, pagination, sort headers)
- Basic filter dropdowns for category, tag
- Limit number of products shown per page
- Sort order and default sorting option
- Add settings page (enable/disable features)
- Add 2 layout templates (grid/table mode)
- Add third tab to metabox for Advanced logic (Pro)
- WordPress.org-ready readme.txt and assets
- Attribute & taxonomy filters
- Product variation display options (e.g., each variation in its own row)
- Quick add-to-cart (bulk add support)
- Shortcode builder with copy-to-clipboard field
- Live preview table panel with mock data
- Visual column layout builder with drag-and-drop
- Save/load basic layout presets
- Sortable animations and drag handles

## Phase 2 – Pro Plugin Features
- Advanced tab in product metabox for conditional logic, badges, and CTA override
- Multi-select add-to-cart / bulk ordering
- Column-level control (width, alignment, visibility)
- Advanced filters (attribute, price slider, ACF fields, taxonomies)
- Role-based visibility (hide/show columns or rows)
- Saved tables and shortcode presets
- Print/PDF/CSV export options
- Wishlist and compare from table
- Conditional logic for columns
- Floating header and sticky cart column
- Lightbox support for product images
- Auto-inclusion of products via category
- Lazy loading and load more
- WC Vendor and ACF field integration
- Template override and custom column rendering via hooks
- REST API endpoints for loading/rendering tables
- Column-level configuration modal per field (e.g. price options, image size, etc.)
- User-defined layout presets with export/import support
- Drag handles and icons for better UI feedback
- Sortable animation effects
- Live preview with mock product data and custom sample control

## Phase 3 – AI Enhancements
- AI-powered product suggestions inside table
- AI-based search prediction/autocomplete
- AI optimizer: reorder or hide irrelevant products automatically
- Inline AI FAQ/chatbot support

## Phase 4 – Developer & Business Expansion
- REST API endpoints
- WPML/Polylang compatibility
- Hooks/filters documentation
- SaaS/remote sync option for managing tables from dashboard
- Multi-site compatibility

# Competitive Differentiators (vs. Other Product Table Plugins)

These features will clearly distinguish Smart Product Table from popular plugins like “Woo Product Table” and others:

- ✅ Visual drag-and-drop layout builder (instead of static shortcode)
- ✅ Column-specific settings panel (e.g., image size, price options)
- ✅ Real-time live preview panel using mock product data
- ✅ Shortcode metabox with copy-to-clipboard support
- ✅ Preset layout templates (save/load custom layouts)
- ✅ Modern UI/UX with WordPress-standard styling
- ✅ Icons and drag handles for better interaction
- ✅ Sortable animations and responsive behaviors
- 🟡 Future: User-defined export options (CSV, PDF)
- 🟡 Future: AI layout suggestions and optimization
- 🟡 Future: Mobile-specific column visibility controls
- 🟡 Future: Column styling (font, background, width)
- 🟡 Future: Table usage analytics (click/view tracking)